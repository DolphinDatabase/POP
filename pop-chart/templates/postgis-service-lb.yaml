apiVersion: v1
kind: Service
metadata:
  name: postgres-headless-svc
spec:
  type: LoadBalancer
  clusterIP: None
  ports:
  - name: postgresql
    port: 5432
    targetPort: postgresql
    protocol: TCP 
  selector:
    app: database